<%= form_with(model: contract_event_alert, local: true) do |form| %>
  <% if contract_event_alert.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(contract_event_alert.errors.count, "error") %> prohibited this contract_event_alert from being saved:</h2>

      <ul>
      <% contract_event_alert.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :contract_event_id %>
    <%= form.text_field :contract_event_id %>
  </div>

  <div class="field">
    <%= form.label :titulo %>
    <%= form.text_field :titulo %>
  </div>

  <div class="field">
    <%= form.label :modified_at %>
    <%= form.datetime_select :modified_at %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
